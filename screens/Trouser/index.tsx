import React, { FC } from 'react';
import { StyleSheet, SafeAreaView } from 'react-native';

import theme, { Box, Text } from '../../components/Themed';

const styles = StyleSheet.create({
  container: {},
});

const Trouser = (): JSX.Element => {
  return (
    <Box>
      <Text>Trouser</Text>
    </Box>
  );
};

export default Trouser;
